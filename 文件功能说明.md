# 文件功能说明文档

本文档说明项目中关键文件的功能和作用。

---

## 📁 核心文件概览

### 1. `app/api/tutorials/create/route.ts` - 教程创建 API

**功能**：处理用户创建新教程的请求

**主要流程**：
1. **用户认证**：验证用户是否已登录
2. **输入验证**：检查用户输入的文本（如"第一次去健身房"）
3. **智能内容生成**：
   - **优先使用预生成数据**：先检查 `pre_generated_tutorials` 表中是否有该输入的预生成教程
   - **如果没有预生成**：调用 AI API 实时生成教程内容
4. **数据存储**：
   - 创建 `tutorial_instances` 记录（教程实例）
   - 插入 `steps` 表（步骤列表，6-7个步骤）
   - 插入 `items` 表（准备物品清单）
5. **返回结果**：返回新创建的教程 ID

**关键特性**：
- 支持预生成缓存机制，提升响应速度
- 自动调用 AI 生成结构化教程内容
- 完整的错误处理

---

### 2. `app/api/tutorials/pre-generate/route.ts` - 预生成教程 API

**功能**：提前生成热门活动的教程内容，存储在数据库中

**为什么需要**：
- 用户创建教程时，如果使用预生成数据，可以**立即返回**，无需等待 AI API 响应
- 提升用户体验，减少等待时间
- 降低 AI API 调用成本

**主要方法**：

#### POST 方法 - 预生成单个教程
- 接收一个 `input_text`（如"第一次去健身房"）
- 检查是否已存在预生成数据
- 如果不存在，调用 AI 生成并存储到 `pre_generated_tutorials` 表
- 返回预生成结果

#### PUT 方法 - 批量预生成
- 接收一个活动数组 `activities: string[]`
- 分批处理（每批3个，避免过载）
- 返回成功和失败的结果统计

**使用场景**：
- 管理员可以提前预生成热门活动的教程
- 在用户创建教程时，`create/route.ts` 会优先使用这些预生成数据

---

### 3. `app/tutorial/[id]/ClientTutorialPage.tsx` - 教程页面组件

**功能**：显示教程详情页面，用户在此完成教程步骤

**主要功能模块**：

#### 数据加载
- 从 API 获取教程数据（标题、步骤、物品）
- 使用 Supabase Realtime 订阅步骤更新（实时同步）

#### UI 展示
- **标题区域**：显示教程标题
- **进度条**：显示完成进度（已完成步骤数 / 总步骤数）
- **准备物品区域**：可展开/收缩的物品清单
- **步骤列表**：每个步骤支持：
  - 右滑完成（`SwipeableStep` 组件`）
  - 点击查看详情（打开模态框）
  - 显示完成状态

#### 完成功能
- **单个步骤完成**：
  - 右滑触发完成
  - 乐观更新（立即显示完成状态）
  - 2秒撤销窗口
  - 后台同步到服务器
  
- **一键完成**（长按按钮）：
  - 长按按钮1秒后触发
  - 显示长按进度条
  - 批量完成所有未完成的步骤（不显示动画）
  - 直接跳转到完成页面
  
- **全部完成后的点击**：
  - 如果所有步骤都已通过右滑完成
  - 点击按钮直接跳转到完成页面

#### 实时同步
- 使用 Supabase Realtime 监听步骤更新
- 跨设备同步完成状态

---

### 4. `architecture.md` - 项目架构文档

**功能**：项目的整体架构设计文档

**包含内容**：
1. **项目概览**：产品简述和核心功能
2. **文件结构**：推荐的项目目录结构
3. **各部分作用**：每个模块的详细说明
4. **状态存储**：客户端/服务器/数据库的数据存储策略
5. **服务与连接**：API 设计和交互流程
6. **数据库 Schema**：数据表结构设计
7. **安全与权限**：Row Level Security (RLS) 和权限控制
8. **部署建议**：CI/CD 和监控方案
9. **可选增强**：缓存、离线、多语言等功能

**作用**：
- 为开发者提供项目整体架构参考
- 指导新功能开发
- 文档化设计决策

---

## 🔄 文件之间的交互流程

### 创建教程流程

```
用户输入 "第一次去健身房"
    ↓
[首页] InputFirstThing 组件
    ↓
POST /api/tutorials/create
    ↓
[create/route.ts]
    ├─ 检查预生成数据 (pre_generated_tutorials 表)
    │   ├─ 有 → 使用预生成数据
    │   └─ 无 → 调用 AI API 生成
    ├─ 创建 tutorial_instances
    ├─ 插入 steps
    └─ 插入 items
    ↓
返回 tutorialId
    ↓
跳转到 /tutorial/[id]
    ↓
[ClientTutorialPage.tsx]
    ├─ 加载教程数据
    ├─ 显示步骤列表
    └─ 用户完成步骤
```

### 预生成流程

```
管理员/脚本
    ↓
POST /api/tutorials/pre-generate
    ↓
[pre-generate/route.ts]
    ├─ 调用 AI API 生成内容
    └─ 存储到 pre_generated_tutorials 表
    ↓
后续用户创建相同教程时
    ↓
[create/route.ts] 直接使用预生成数据
    ↓
快速响应，无需等待 AI
```

### 完成教程流程

```
用户在 ClientTutorialPage
    ↓
方式1: 逐个右滑完成步骤
    ├─ 调用 PATCH /api/tutorials/[id]/steps/[stepId]/complete
    └─ 实时更新进度
    ↓
方式2: 长按完成按钮（1秒）
    ├─ 批量完成所有步骤
    └─ 调用 PATCH /api/tutorials/[id]/complete
    ↓
跳转到完成页面
    ├─ 创建勋章
    └─ 显示完成动画
```

---

## 🎯 关键设计决策

### 1. 预生成机制
- **目的**：提升用户体验，减少等待时间
- **实现**：提前生成热门教程，存储在数据库中
- **优势**：快速响应、降低成本

### 2. 乐观更新
- **目的**：提升交互流畅度
- **实现**：步骤完成时立即更新 UI，后台同步
- **优势**：用户无需等待网络请求

### 3. 长按一键完成
- **目的**：提供快速完成选项
- **实现**：长按1秒，批量完成所有步骤
- **优势**：适合想要快速完成的用户

### 4. 实时同步
- **目的**：支持多设备同步
- **实现**：Supabase Realtime 订阅
- **优势**：跨设备状态一致

---

## 📊 数据表关系

```
tutorial_instances (教程实例)
    ├─ id (主键)
    ├─ user_id (用户ID)
    ├─ input_text (用户输入)
    ├─ title, description, tags, difficulty
    └─ progress, completed, completed_at
        ↓
    steps (步骤)
        ├─ tutorial_id (外键)
        ├─ ord (顺序)
        ├─ title, summary, detail_prompt
        └─ completed, completed_at
    ↓
    items (物品)
        ├─ tutorial_id (外键)
        └─ name, qty, note

pre_generated_tutorials (预生成教程)
    ├─ input_text (唯一标识)
    ├─ title, description, tags, difficulty
    └─ tutorial_data (JSONB: steps + items)
```

---

## 🔧 技术栈

- **前端框架**：Next.js (App Router)
- **UI 库**：React + Tailwind CSS
- **数据库**：Supabase (PostgreSQL)
- **实时同步**：Supabase Realtime
- **AI 服务**：硅基流动 (SiliconFlow) / DeepSeek
- **认证**：Supabase Auth

---

## 📝 总结

这四个文件构成了教程系统的核心：

1. **create/route.ts** - 创建教程的入口，智能选择预生成或实时生成
2. **pre-generate/route.ts** - 预生成系统，提升性能和用户体验
3. **ClientTutorialPage.tsx** - 教程展示和交互的核心组件
4. **architecture.md** - 项目架构文档，指导开发

它们共同实现了一个完整的"第一次体验"教程系统，支持 AI 生成、实时同步、快速完成等功能。

