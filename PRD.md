# 我的第一次 - 产品需求文档 (PRD)

## 1. 产品概述

### 1.1 产品定位
"我的第一次"（First-Go）是一款基于 AI 技术的个性化教程生成应用，帮助用户记录和完成人生中的各种"第一次"体验。通过 AI 自动生成结构化的教程步骤，引导用户顺利完成目标，并提供进度追踪、勋章激励等功能。

### 1.2 产品愿景
让每个人都能轻松迈出第一步，将"第一次"的恐惧转化为系统化的行动指南。

### 1.3 核心价值
- **降低门槛**：通过 AI 生成的详细教程，让用户不再为"如何开始"而困扰
- **结构化指导**：将复杂的任务拆分为清晰的步骤，逐步完成
- **成就感激励**：通过进度追踪和勋章系统，增强用户的完成动机

## 2. 目标用户

### 2.1 主要用户群体
- **新手探索者**：想要尝试新事物但不知从何开始的用户
- **目标导向型用户**：有明确目标但需要结构化指导的用户
- **习惯养成者**：希望通过系统化方法养成新习惯的用户

### 2.2 用户画像
- 年龄：18-35 岁
- 特征：对新技术接受度高，愿意尝试 AI 辅助工具
- 需求：需要清晰的步骤指导，希望获得成就感反馈

## 3. 核心功能

### 3.1 用户认证系统
**功能描述**：用户注册、登录、登出功能

**需求详情**：
- 支持邮箱密码注册/登录
- 使用 Supabase Auth 进行身份认证
- 登录状态持久化
- 未登录用户自动跳转到登录页

**优先级**：P0（必需）

---

### 3.2 AI 教程生成
**功能描述**：基于用户输入生成个性化教程

**需求详情**：
- 用户输入"我第一次想做什么"（例如："第一次健身"）
- AI 自动生成包含以下内容的教程：
  - **标题**：生活化、清晰的教程标题
  - **描述**：一句概括全流程的鼓励性话语（80字以内）
  - **物品清单**：3-5 个具体、实用的关键物品
  - **步骤列表**：6-7 个步骤，每个步骤包含：
    - 步骤标题
    - 步骤简介（80字以内）
  - **标签**：3 个相关标签
  - **难度**：1-5 之间的难度数字
- 支持预生成功能：针对常用活动（健身、做饭、旅行等）提前生成教程，节省 API 配额
- 首次加载时自动预生成热门活动教程

**AI 服务**：
- 当前支持：SiliconFlow / DeepSeek
- API 调用统一封装在 `lib/ai.ts`
- Prompt 统一管理在 `buildTutorialPrompt` 函数

**优先级**：P0（必需）

---

### 3.3 教程浏览与交互
**功能描述**：查看教程详情，管理步骤完成状态

**需求详情**：
- **教程页面**：
  - 显示教程标题、描述
  - 显示物品清单（可展开/收缩）
  - 显示步骤列表，每个步骤显示：
    - 步骤序号
    - 步骤标题
    - 步骤简介（可选）
    - 完成状态（已完成/未完成）
  - 实时显示教程进度（已完成步骤数 / 总步骤数）
  
- **步骤交互**：
  - **右滑完成**：在移动端支持右滑手势标记步骤完成
  - **撤销功能**：完成步骤后 2 秒内可撤销
  - **步骤详情**：点击步骤可查看详细说明
    - 如果已有详情，直接显示
    - 如果没有详情，自动或手动触发 AI 生成详细说明
    - 详情以 Markdown 格式展示，支持标题、段落、列表等

**优先级**：P0（必需）

---

### 3.4 步骤详情生成
**功能描述**：为每个步骤生成详细的说明内容

**需求详情**：
- 当用户点击步骤详情时：
  - 如果数据库中已有详情，直接展示
  - 如果没有，调用 AI API 生成详细说明
  - 生成过程中显示加载状态
  - 生成完成后自动保存到数据库
  - 详情以 Markdown 格式存储和展示
- 支持手动触发生成（通过"生成详细说明"按钮）
- 生成的内容应包括：
  - 详细的步骤说明
  - 注意事项
  - 可能的问题和解决方案

**优先级**：P0（必需）

---

### 3.5 进度追踪
**功能描述**：实时追踪教程完成进度

**需求详情**：
- 在教程页面显示进度条
- 进度计算公式：已完成步骤数 / 总步骤数
- 当步骤完成状态改变时，自动更新进度
- 进度同步到数据库（`tutorial_instances.progress`）
- 在历史记录页面显示每个教程的进度

**优先级**：P0（必需）

---

### 3.6 勋章系统
**功能描述**：完成教程后获得勋章奖励

**需求详情**：
- **勋章获取**：
  - 当用户完成所有步骤时，自动获得勋章
  - 勋章与教程标题相关联，支持多种类型（健身、做饭、旅行等）
  - 支持同一教程多次完成，显示完成次数和第一次完成时间
  
- **勋章展示**：
  - **勋章墙页面**：
    - 展示所有已获得的勋章
    - 根据教程类型显示不同的 emoji（💪健身、👨‍🍳做饭、📚学习等）
    - 根据教程类型播放不同的动画视频：
      - 游泳相关：`swim.mp4`
      - 唱歌相关：`sing.mp4`
      - 画画相关：`paint.mp4`
      - 其他：`xunzhang.mov`
    - 合并相同教程的多次完成记录
    - 显示完成次数和首次完成时间
  
- **勋章类型**：
  - 根据教程标题自动识别类型
  - 支持自定义 emoji 映射

**优先级**：P1（重要）

---

### 3.7 历史记录
**功能描述**：查看所有已创建的教程

**需求详情**：
- 显示所有教程列表（按创建时间倒序）
- 每个教程显示：
  - 教程标题
  - 输入文字
  - 进度条（已完成步骤数 / 总步骤数）
  - 创建时间
  - 完成状态（已完成/进行中）
- 点击教程卡片跳转到教程详情页
- 支持筛选（已完成/进行中）
- 空状态提示

**优先级**：P1（重要）

---

### 3.8 快速启动
**功能描述**：提供常用活动的快速入口

**需求详情**：
- 在首页显示木块堆叠的快速启动区域
- 预设活动：
  - 健身（1块）
  - 做饭、旅行（2块）
  - 蹦极、购物、开车（3块）
  - 画画、唱歌、读书、拼豆（4块）
- 点击木块自动填充输入框并创建教程
- 如果该活动已预生成，直接使用预生成数据，避免重复 API 调用

**优先级**：P1（重要）

---

### 3.9 完成流程
**功能描述**：教程全部完成后的庆祝页面

**需求详情**：
- 当所有步骤完成时，显示完成页面
- 显示祝贺信息
- 展示获得的勋章
- 提供返回首页或查看历史记录的选项

**优先级**：P2（可选）

## 4. 用户流程

### 4.1 新用户注册流程
```
1. 访问首页
2. 未登录，自动跳转到登录页
3. 选择"注册"
4. 输入邮箱和密码
5. 点击注册
6. 注册成功，跳转到首页
```

### 4.2 创建教程流程
```
1. 在首页输入"我第一次想做什么"
   OR 点击快速启动木块
2. 点击"开始计划"按钮
3. 系统检查是否有预生成数据
   - 如果有，直接使用
   - 如果没有，调用 AI API 生成
4. 生成成功后跳转到教程详情页
```

### 4.3 完成教程流程
```
1. 在教程详情页查看步骤列表
2. 点击步骤查看详情（可选）
3. 完成步骤：
   - 移动端：右滑标记完成
   - 桌面端：点击完成按钮
4. 进度条实时更新
5. 所有步骤完成后：
   - 自动获得勋章
   - 跳转到完成页面
   - 显示祝贺信息
```

### 4.4 查看历史记录流程
```
1. 点击首页右上角历史记录图标
2. 查看所有教程列表
3. 点击教程卡片查看详情
4. 继续未完成的教程
```

### 4.5 查看勋章流程
```
1. 点击首页右上角勋章图标
2. 查看所有已获得的勋章
3. 查看完成次数和完成时间
4. 观看勋章动画（根据类型）
```

## 5. 技术架构

### 5.1 技术栈
- **前端框架**：Next.js 16 (App Router) + React 19
- **编程语言**：TypeScript
- **样式方案**：Tailwind CSS v4
- **后端服务**：Supabase
  - 认证：Supabase Auth
  - 数据库：PostgreSQL
  - 实时功能：Supabase Realtime（如需要）
- **AI 服务**：SiliconFlow / DeepSeek（通过 OpenAI 兼容 API）
- **部署平台**：Vercel
- **错误监控**：Sentry（可选）

### 5.2 项目结构
```
/app                    # Next.js App Router 页面
  /api                  # API 路由
    /tutorials          # 教程相关 API
    /badges             # 勋章相关 API
    /history            # 历史记录 API
    /admin              # 管理功能 API
  /tutorial/[id]        # 教程详情页
  /tutorial/[id]/step/[stepId]  # 步骤详情页
  /badges               # 勋章墙页面
  /history              # 历史记录页面
  /login                # 登录页面
/components             # React 组件
  AuthGuard.tsx         # 认证守卫
  InputFirstThing.tsx   # 输入组件
  StepCard.tsx          # 步骤卡片
  SwipeableStep.tsx     # 可滑动步骤组件
  ModalDetail.tsx       # 详情弹窗
  StepDetailContent.tsx # 步骤详情内容
  TutorialProgress.tsx  # 教程进度组件
  LoadingSpinner.tsx    # 加载动画
/lib                    # 工具函数
  ai.ts                 # AI 调用逻辑
  supabaseClient.ts     # Supabase 客户端
  serverSupabase.ts     # 服务端 Supabase
/types                  # TypeScript 类型定义
/scripts                # SQL 脚本
  schema.sql            # 数据库 schema
  rls.sql               # 行级安全策略
  profiles_trigger.sql  # 用户 profile 触发器
```

### 5.3 数据库设计

#### 5.3.1 核心表结构

**profiles**（用户资料表）
- `id` (uuid, PK): 用户 ID，关联 auth.users
- `display_name` (text): 显示名称
- `avatar_url` (text): 头像 URL
- `created_at` (timestamptz): 创建时间

**tutorial_instances**（教程实例表）
- `id` (uuid, PK): 教程 ID
- `user_id` (uuid, FK): 用户 ID
- `input_text` (text): 用户输入的原始文字
- `title` (text): 教程标题
- `description` (text): 教程描述
- `tags` (text[]): 标签数组
- `difficulty` (text): 难度等级
- `progress` (numeric): 进度（0-1）
- `completed` (boolean): 是否完成
- `created_at` (timestamptz): 创建时间
- `completed_at` (timestamptz): 完成时间

**steps**（步骤表）
- `id` (uuid, PK): 步骤 ID
- `tutorial_id` (uuid, FK): 教程 ID
- `ord` (int): 步骤顺序
- `title` (text): 步骤标题
- `summary` (text): 步骤简介
- `detail` (text): 步骤详细说明（Markdown）
- `detail_prompt` (text): 生成详情时使用的 prompt
- `completed` (boolean): 是否完成
- `completed_at` (timestamptz): 完成时间
- `created_at` (timestamptz): 创建时间

**items**（物品表）
- `id` (uuid, PK): 物品 ID
- `tutorial_id` (uuid, FK): 教程 ID
- `name` (text): 物品名称
- `qty` (text): 数量
- `note` (text): 备注

**badges**（勋章表）
- `id` (uuid, PK): 勋章 ID
- `key` (text, unique): 勋章唯一标识
- `title` (text): 勋章标题
- `description` (text): 勋章描述
- `icon_url` (text): 图标 URL
- `created_at` (timestamptz): 创建时间

**user_badges**（用户勋章表）
- `id` (uuid, PK): 记录 ID
- `user_id` (uuid, FK): 用户 ID
- `badge_id` (uuid, FK): 勋章 ID
- `awarded_at` (timestamptz): 获得时间
- `source_tutorial` (uuid, FK): 来源教程 ID

**pre_generated_tutorials**（预生成教程表）
- `id` (uuid, PK): 记录 ID
- `input_text` (text, unique): 输入文字（唯一）
- `title` (text): 教程标题
- `description` (text): 教程描述
- `items` (jsonb): 物品列表（JSON）
- `steps` (jsonb): 步骤列表（JSON）
- `tags` (text[]): 标签数组
- `difficulty` (text): 难度等级
- `created_at` (timestamptz): 创建时间

**ai_responses**（AI 响应审计表）
- `id` (uuid, PK): 记录 ID
- `user_id` (uuid, FK): 用户 ID
- `prompt` (text): 请求 prompt
- `response` (jsonb): AI 响应（JSON）
- `created_at` (timestamptz): 创建时间

#### 5.3.2 关系说明
- 一个用户可以有多个教程实例（1:N）
- 一个教程实例可以有多个步骤（1:N）
- 一个教程实例可以有多个物品（1:N）
- 一个用户可以获得多个勋章（N:M）
- 一个教程实例可以触发一个勋章（1:1）

### 5.4 API 设计

#### 5.4.1 教程相关 API

**POST /api/tutorials/create**
- 功能：创建新教程
- 请求体：`{ input_text: string }`
- 响应：`{ tutorialId: string }`
- 逻辑：
  1. 检查 `pre_generated_tutorials` 表
  2. 如果有预生成数据，直接使用
  3. 如果没有，调用 AI API 生成
  4. 保存到数据库

**GET /api/tutorials/[id]**
- 功能：获取教程详情
- 响应：教程完整信息（包括步骤、物品）

**POST /api/tutorials/[id]/complete**
- 功能：标记教程完成
- 逻辑：检查所有步骤是否完成，更新教程状态

**POST /api/tutorials/[id]/steps/[stepId]/complete**
- 功能：标记步骤完成/未完成
- 请求体：`{ completed: boolean }`
- 逻辑：
  1. 更新步骤完成状态
  2. 重新计算教程进度
  3. 更新 `tutorial_instances.progress`
  4. 如果所有步骤完成，触发勋章授予

**POST /api/tutorials/[id]/steps/[stepId]/generate-detail**
- 功能：生成步骤详细说明
- 响应：`{ detail: string }`
- 逻辑：
  1. 检查步骤是否已有详情
  2. 如果没有，调用 AI API 生成
  3. 保存到数据库
  4. 返回详情

#### 5.4.2 勋章相关 API

**GET /api/badges**
- 功能：获取用户所有勋章
- 响应：勋章列表（包括完成次数、首次完成时间）

#### 5.4.3 历史记录相关 API

**GET /api/history**
- 功能：获取用户所有教程
- 响应：教程列表（包括进度、完成状态）

#### 5.4.4 预生成相关 API

**POST /api/tutorials/pre-generate**
- 功能：预生成单个教程
- 请求体：`{ input_text: string }`

**PUT /api/tutorials/pre-generate**
- 功能：批量预生成教程
- 请求体：`{ activities: string[] }`

**POST /api/admin/init-pre-generated**
- 功能：初始化预生成教程（管理员）
- 逻辑：批量预生成常用活动教程

## 6. UI/UX 设计

### 6.1 设计风格
- **风格**：像素风格（Pixel Art）
- **配色方案**：
  - 主背景：`#f5f0e8`（米色）
  - 文字：`#6b5335`（棕色）
  - 边框：`#8b6f47`（深棕色）
  - 按钮：木纹风格
- **字体**：像素字体（Pixel Font）
- **图标**：像素风格 PNG 图标

### 6.2 核心组件

#### 6.2.1 首页
- **布局**：
  - 顶部：勋章图标、历史记录图标
  - 中间：对话气泡（牛仔角色）+ 木块堆叠（快速启动）
  - 底部：输入框（"第一次"输入）
- **交互**：
  - 点击木块自动填充输入框
  - 输入后点击"开始计划"创建教程

#### 6.2.2 教程详情页
- **布局**：
  - 顶部：返回按钮、教程标题、描述
  - 中间：物品清单（可展开/收缩）
  - 底部：步骤列表、进度条
- **交互**：
  - 点击步骤查看详情（弹窗）
  - 右滑/点击完成步骤
  - 完成所有步骤后跳转到完成页面

#### 6.2.3 步骤详情弹窗
- **布局**：
  - 顶部：关闭按钮、步骤标题
  - 内容：步骤详细说明（Markdown 渲染）
- **交互**：
  - 如果没有详情，显示"生成详细说明"按钮
  - 生成中显示加载状态
  - 生成完成后自动显示内容

#### 6.2.4 历史记录页
- **布局**：
  - 顶部：标题、返回按钮
  - 列表：教程卡片（标题、进度条、创建时间）
- **交互**：
  - 点击卡片跳转到教程详情页

#### 6.2.5 勋章墙页
- **布局**：
  - 顶部：标题、返回按钮
  - 网格：勋章卡片（emoji、标题、完成次数、完成时间）
- **交互**：
  - 点击勋章播放动画
  - 显示完成次数和首次完成时间

### 6.3 响应式设计
- **移动端优先**：主要针对手机屏幕优化
- **桌面端适配**：支持桌面浏览器访问
- **手势支持**：移动端支持右滑完成步骤

### 6.4 加载状态
- **骨架屏**：教程页面加载时显示骨架屏
- **加载动画**：使用 LoadingSpinner 组件
- **错误处理**：显示友好的错误提示

## 7. 非功能性需求

### 7.1 性能要求
- **首屏加载时间**：< 2 秒
- **AI 生成响应时间**：< 10 秒（可接受）
- **页面切换流畅度**：60 FPS
- **API 响应时间**：< 500ms（数据库查询）

### 7.2 可用性要求
- **浏览器兼容性**：支持 Chrome、Safari、Firefox、Edge（最新版本）
- **移动端兼容性**：支持 iOS 12+、Android 8+
- **网络容错**：弱网环境下显示友好提示

### 7.3 安全性要求
- **身份认证**：使用 Supabase Auth，支持 JWT
- **数据隔离**：使用 RLS（Row Level Security）确保用户只能访问自己的数据
- **API 安全**：所有 API 路由验证用户身份
- **输入验证**：前端和后端双重验证

### 7.4 可维护性要求
- **代码规范**：使用 TypeScript、ESLint
- **类型安全**：完整的 TypeScript 类型定义
- **错误监控**：集成 Sentry（可选）
- **日志记录**：记录关键操作和错误

### 7.5 扩展性要求
- **AI 服务切换**：支持切换不同的 AI 服务提供商
- **功能扩展**：预留扩展接口（社交分享、自定义步骤等）
- **多语言支持**：代码结构支持国际化（未来）

## 8. 数据流

### 8.1 教程创建流程
```
用户输入 → 前端验证 → API 路由 → 检查预生成表 → 
  → 有数据：直接使用
  → 无数据：调用 AI API → 解析响应 → 保存到数据库 → 
返回教程 ID → 前端跳转到教程页
```

### 8.2 步骤完成流程
```
用户完成步骤 → 前端发送请求 → API 路由 → 更新步骤状态 → 
计算教程进度 → 更新教程进度 → 检查是否全部完成 → 
  → 全部完成：授予勋章 → 返回成功
  → 未完成：返回成功
前端更新 UI
```

### 8.3 步骤详情生成流程
```
用户点击步骤详情 → 检查是否有详情 → 
  → 有详情：直接显示
  → 无详情：调用 AI API → 生成详情 → 保存到数据库 → 
返回详情 → 前端显示
```

## 9. 异常处理

### 9.1 网络错误
- **AI API 调用失败**：显示错误提示，允许重试
- **数据库连接失败**：显示错误提示，记录日志
- **网络超时**：显示超时提示，允许重试

### 9.2 数据错误
- **AI 返回格式错误**：记录错误，显示友好提示
- **数据库约束错误**：显示错误提示，记录日志
- **权限错误**：跳转到登录页

### 9.3 用户体验错误
- **输入验证失败**：显示具体错误信息
- **操作冲突**：防止重复提交，显示加载状态

## 10. 未来规划

### 10.1 短期计划（1-3 个月）
- [ ] 接入真实 AI 服务（OpenAI/其他）
- [ ] 优化 AI prompt 以提高教程质量
- [ ] 添加更多勋章类型和动画
- [ ] 改进移动端体验
- [ ] 添加社交分享功能
- [ ] 支持自定义步骤编辑

### 10.2 中期计划（3-6 个月）
- [ ] 添加教程分类和搜索
- [ ] 支持教程收藏和标签管理
- [ ] 添加用户个人资料编辑
- [ ] 实现教程模板功能
- [ ] 添加社区功能（分享教程、评论）

### 10.3 长期计划（6-12 个月）
- [ ] 多语言支持（国际化）
- [ ] 移动端原生应用（React Native）
- [ ] 高级功能（数据分析、个性化推荐）
- [ ] 企业版功能（团队协作、数据导出）

## 11. 成功指标

### 11.1 用户指标
- **用户注册数**：月增长率 > 20%
- **日活跃用户数（DAU）**：目标 > 1000
- **用户留存率**：7 日留存 > 30%

### 11.2 功能指标
- **教程创建数**：日均 > 100
- **教程完成率**：> 40%
- **步骤详情查看率**：> 60%

### 11.3 技术指标
- **API 响应时间**：P95 < 500ms
- **错误率**：< 1%
- **AI 生成成功率**：> 95%

## 12. 风险评估

### 12.1 技术风险
- **AI API 成本**：通过预生成功能降低 API 调用次数
- **数据库性能**：使用索引和查询优化
- **第三方服务依赖**：Supabase、AI 服务的稳定性

### 12.2 产品风险
- **用户需求不明确**：通过用户反馈持续迭代
- **竞争产品**：差异化功能（像素风格、快速启动）

### 12.3 运营风险
- **内容质量**：优化 AI prompt，提高生成质量
- **用户流失**：通过勋章系统和进度追踪提高参与度

## 13. 附录

### 13.1 术语表
- **教程实例（Tutorial Instance）**：用户创建的一个教程
- **步骤（Step）**：教程中的一个步骤
- **物品（Item）**：教程所需的物品清单
- **勋章（Badge）**：完成教程后获得的奖励
- **预生成（Pre-generation）**：提前生成常用教程，节省 API 配额

### 13.2 相关文档
- [README.md](./README.md)：项目说明
- [DEPLOY.md](./DEPLOY.md)：部署指南
- [PUBLISH.md](./PUBLISH.md)：发布清单
- [MIGRATION.md](./MIGRATION.md)：迁移指南

### 13.3 联系方式
- **项目仓库**：GitHub
- **问题反馈**：GitHub Issues
- **技术支持**：项目维护者

---

**文档版本**：v1.0  
**最后更新**：2024年  
**维护者**：开发团队

